# script to init updated env at a start.

# ---------------------------- My Aliases ----------------------------
Set-Alias -Name 'touch' -Value New-Item 
Set-Alias -Name '>' -Value Set-Content
Set-Alias -Name '>>' -Value Add-Contentecho